# reply-ai

AI app that generates message replies (professional, friendly, direct).

## Run locally

Open `index.html` in a browser, or serve the directory with a simple static file server such as:

```bash
python3 -m http.server 8000
```
